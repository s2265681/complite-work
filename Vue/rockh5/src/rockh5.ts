import { Btn, BtnGroup } from './packages/Btn'
import {
  transDateStr, formatDate,
  outclick,
  clone, mod, sleep, getNow, pad,
  decode, encode, qsParse, qsStringify, escapeHtml, escapeJs,
  throttle, debounce,
  px2vw, realPx, targetPx,
  isAndroid, isiOS, isIPX
} from './packages/Utils'


export {
  Btn, BtnGroup,
  transDateStr, formatDate,
  outclick,
  clone, mod, sleep, getNow, pad,
  decode, encode, qsParse, qsStringify, escapeHtml, escapeJs,
  throttle, debounce,
  px2vw, realPx, targetPx,
  isAndroid, isiOS, isIPX
}